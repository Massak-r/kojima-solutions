// Deprecated — use `@/lib/statusColors` instead. Kept as a re-export shim so
// existing imports (ObjectiveCard, SubtaskCard, ObjectiveRow, etc.) keep
// working until they're migrated to point at statusColors directly.
export * from "./statusColors";
