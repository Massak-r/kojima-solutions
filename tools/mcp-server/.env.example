# Copy to .env (not committed) or pass these via `claude mcp add --env`.

# Production base. Override to http://localhost:8080 for local dev.
KOJIMA_API_BASE=https://kojima-solutions.ch

# Same value as API_SECRET in public/api/config.php on the server.
KOJIMA_API_KEY=
