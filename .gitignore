# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# API credentials (never commit)
public/api/config.php
.env
.env.*

# Uploaded files (user data, not source)
public/uploads/
public/private_docs/
public/private_videos/

# Local-first journals (per-project markdown + inbox)
# Comment out the next line to opt-in to git-syncing your journals (e.g., for
# mobile editing via Working Copy / Obsidian Sync).
.kojima-journal/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.claude/settings.local.json
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
